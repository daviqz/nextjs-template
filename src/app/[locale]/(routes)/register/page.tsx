import { RegisterForm } from '@/app/components/page/RegisterForm/RegisterForm'

const RegisterPage = async () => {
	return (
		<div className='flex h-screen w-screen items-center justify-center'>
			<RegisterForm />
		</div>
	)
}

export default RegisterPage
